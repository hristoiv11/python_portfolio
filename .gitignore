# Ignore PyCharm project files
.idea/

# Ignore environment files
.env
*.env  # Covers all .env files in any folder
frontend/.env

# Exclude data folder contents (for MongoDB data)
data/*
!data/database/*.cnf
!data/database/binlog.index
!data/database/ib_buffer_pool
*.sock

# Backend (Django) exclusions
backend/.idea/
backend/.venv/
backend/__pycache__/
backend/db.sqlite3  # If using SQLite for testing
backend/media/  # Ignore uploaded media files
backend/staticfiles/  # Ignore collected static files
backend/logs/

# Frontend (React + TypeScript) exclusions
frontend/node_modules/
frontend/dist/
frontend/.next/  # If using Next.js
frontend/.cache/

# Package manager lock files (only keep relevant one)
package-lock.json  # If npm is used instead of yarn

# Docker-specific exclusions
docker-compose.override.yml

# System files
*.DS_Store
Thumbs.db
